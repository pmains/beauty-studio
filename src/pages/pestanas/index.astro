---
import ServiceLayout from '@/layouts/ServiceLayout.astro';
import studio from '@/data/services.json';

const category = studio.categories.find(c => c.id === 'pestanas')!;
---

<ServiceLayout category={category} />
